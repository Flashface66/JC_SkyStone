package Club_Meeting;

import com.qualcomm.robotcore.eventloop.opmode.LinearOpMode;
import com.qualcomm.robotcore.eventloop.opmode.TeleOp;


@TeleOp
public class Results extends LinearOpMode {

    /*
    Variables to be placed her, this includes but is not limited to:
    Motors, Servos, Sensors, Variables(Double, Float, Boolean, String)
     */

    @Override
    public void runOpMode(){

        /*
        In here Motors, Servos and Sensors are assigned a name to be viewed in the configuration of
        the robot control phone.
         */
        waitForStart();

        while (opModeIsActive()){
            /*
             This is where the body of the code is placed. This is where each object is assigned a
             'Task'. Either the code is written in here or a function is placed here with the code.
             */
        }




    }
}
